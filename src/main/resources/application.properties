server.port=8080
app.environment=${ENVIRONMENT_NAME:local}

# Environment-specific (override in application-{profile}.properties)
app.api.base-url=http://localhost:8080
app.logging.level=INFO
app.feature.debug-enabled=false
app.timeout.seconds=30
